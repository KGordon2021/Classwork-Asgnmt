<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All Books</h1>
    <div class="nav">
        <h2>Search By Genre</h2>
        <nav>
            <a href="">Educational</a>
            <a href="">Fiction</a>
            <a href="">Non-Fiction</a>
            <a href="">Reference Section</a>
        </nav>
        </div>

    <%
         if (my_session.loggedin == true){ %>
    <h3 class="welcome">Welcome <%= my_session.first_Nm %>  </h3>
    <%  
       }
     %> 
    
    <div>            
            <% if(allBooks.length) {
                for(var i = 0; i < allBooks.length; i++) { 
            %>
            <div>
     <form action="/library/request" method="post">

                <div class="imgBx">
                    <img src="" alt="">
                </div>
                <div class="dets">
                <p class="centered"><%= allBooks[i].id %></p>
                <h2><%= allBooks[i].title %></h2>
                <p><%= allBooks[i].subject %></p>           
                <p><%= allBooks[i].genre %> </p>           
                <p><%= allBooks[i].category %> </p>                
                <input type="hidden" value="<%= my_session.student_id %>" name="st_id">
                <input type="hidden" value="<%= allBooks[i].title %>" name="title">
                <input type="hidden" value="<%= new Date().toLocaleDateString('fr-CA') %>" name="date">

                <p class="centered">
                    <a class="light" href="/library/requestedit/<%=allBooks[i].id%>">Read Synopsis</a> <!--this refers to the index of the current data collection which is currently stored as data-->
                    <input type="submit" value="Make a Request">
                </p>
                </div>
</form>
                

            </div>
               <% }
          }else{ %>
            <div>
                <p>No Books currently Listed</p>
            </div>
           <% } %>
</div>

<div class="logout">
    <a href="logout">Logout</a>
    </div>
    </div>
</body>
</html>